#Clone the codecloud
git clone {to clone give the link with https url}

#Chain of cmmands in sequence for commiting the changes
git pull
git status
git add .
git status
git commit -m "add text"
git push

#Playing around with branches/features
git branch feature/{name a feature we want to create}
git switch {point the branch to switch}
git checkout {point the branch to switch}
git push -u origin {point the branch to where we push created in local/current}

#Additional Commands if needed
git --help